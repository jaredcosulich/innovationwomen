%h1 Listing profiles

%table
  %tr
    %th Name
    %th Location
    %th Title
    %th Industry
    %th Twitter
    %th Linkedin
    %th Website
    %th
    %th
    %th

  - @profiles.each do |profile|
    %tr
      %td= link_to(profile.name, profile)
      %td= profile.location
      %td= profile.title
      %td= profile.industry
      %td= profile.twitter
      %td= profile.linkedin
      %td= profile.website
      - if logged_in? && current_user.owns?(profile) || current_user.try(:admin)
        %td= link_to 'Edit', edit_profile_path(profile)
        %td &#124;
        %td= link_to 'Destroy', profile, method: :delete, data: { confirm: 'Are you sure?' }
%br
